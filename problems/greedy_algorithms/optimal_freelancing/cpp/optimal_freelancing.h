#include <unordered_map>
#include <vector>

bool compareJobs(const std::unordered_map<std::string, int> &,
                 const std::unordered_map<std::string, int> &);

int optimalFreelancing(std::vector<std::unordered_map<std::string, int>>);
